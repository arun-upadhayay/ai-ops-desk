{
  "name": "ai-ops-desk",
  "private": true,
  "workspaces": ["backend", "admin", "frontend"],
  "scripts": {
    "infra": "docker compose -f infra/docker-compose.yml up -d",
    "backend": "npm --workspace backend run dev",
    "admin": "npm --workspace admin run dev",
    "frontend": "npm --workspace frontend run dev"
  }
}
